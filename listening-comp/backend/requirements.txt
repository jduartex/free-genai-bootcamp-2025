# Core dependencies
openai==1.12.0
fastapi==0.108.0
uvicorn==0.25.0
pydantic==2.5.2
python-dotenv==1.0.0

# YouTube transcript processing
youtube-transcript-api==0.6.2
langdetect==1.0.9

# Vector embeddings and database
sentence-transformers==2.5.1
numpy==1.26.4
sqlite-utils==3.35.1
sqlalchemy==2.0.25

# Text-to-Speech services
boto3==1.29.6  # For Amazon Polly
google-cloud-texttospeech==2.15.0

# Speech recognition - alternatives for Python 3.13
# openai-whisper==20231117  # Original package - doesn't work with Python 3.13
ffmpeg-python==0.2.0  # Requires FFmpeg to be installed via Homebrew on Mac
pydub==0.25.1

# Japanese language processing
sudachipy==0.6.8
jaconv==0.3.4  # For kana/kanji conversions

# Mac development tools
watchdog==3.0.0  # For automatic reloading during development

# Instead of openai-whisper, you can try one of these approaches:
# 1. Install from GitHub (may work with Python 3.13):
#    pip install git+https://github.com/openai/whisper.git
#
# 2. Use Python 3.10 in a separate environment for Whisper processing
#
# 3. Use only the OpenAI API for speech recognition (requires API key)
